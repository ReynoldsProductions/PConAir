{
  "name": "@pconair/companion-module",
  "version": "0.3.1",
  "description": "Bitfocus Companion module for PC On Air",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "tsc --noEmit",
    "package": "npm run build && npm run validate-manifest && node scripts/pack.js",
    "validate-manifest": "node scripts/validate-manifest.js"
  },
  "dependencies": {
    "@companion-module/base": "^1.8.0",
    "ws": "^8"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/ws": "^8",
    "typescript": "^5.3.0"
  },
  "engines": {
    "node": ">=18"
  },
  "files": [
    "dist",
    "companion",
    "LICENSE",
    "README.md"
  ]
}
